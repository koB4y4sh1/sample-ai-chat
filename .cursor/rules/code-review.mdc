---
description: コードレビュー回答の形式（.github/instructions/code-review と整合）。レビュー・PR 指摘時に参照。
alwaysApply: false
---

# コードレビュー

- 回答は日本語。
- 指摘は先に、重大度順。可能ならファイル名と行番号。
- 事実と推測を分ける。バグ・仕様逸脱・回帰・テスト不足・運用リスクを優先。

接頭辞:

- `[must]` 必ず修正
- `[imo]` 意見（修正必須ではない）
- `[nits]` 軽微
- `[ask]` 質問
- `[fyi]` 参考
