# コードレビュー指示
- コードレビューの回答は日本語で記載する。
- 指摘事項を先に示し、重大度順に並べる。
- 可能な限りファイル名と行番号を付ける。
- 事実と推測を区別する。
- バグ、仕様逸脱、回帰リスク、テスト不足、運用上のリスクを優先する。
- レビューする際には、以下のprefix(接頭辞)を必ずつける。
[must] → かならず変更してね
[imo] → 自分の意見だとこうだけど修正必須ではないよ(in my opinion)
[nits] → ささいな指摘(nitpick)
[ask] → 質問
[fyi] → 参考情報
